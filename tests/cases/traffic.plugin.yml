- name: 🧮 Repositories traffic - Repositories traffic
  uses: gh-metrics/metrics@latest
  with:
    token: MOCKED_TOKEN
    base: repositories
    plugin_traffic: 'yes'
    use_mocked_data: 'yes'
    verify: 'yes'
