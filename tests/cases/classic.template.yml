- name: 📗 Classic template - Example
  uses: gh-metrics/metrics@latest
  with:
    token: MOCKED_TOKEN
    base: header, repositories
    plugin_lines: yes
    use_mocked_data: yes
    verify: yes
